# CRM/User Split — Frontend + Backend Migration Sequence

> Checked into the repo so this survives across sessions (previously only lived
> in `~/.claude/plans/swift-wishing-ritchie.md`, outside the repo, and got
> temporarily lost). This is the authoritative, currently-active migration plan.
> See also `docs/architecture/tradeverse_architecture.png` (target-state diagram)
> and `docs/architecture/cloudflare-split-plan.md` (earlier draft — superseded by
> this document; kept only as background reading, not a second source of truth).

## Context

The target Cloudflare architecture (already diagrammed and priced) calls for a CRM Backend and User Backend running as separate services instead of one shared FastAPI monolith, mirroring `signals-service/`, which is already split out. This plan extends that same split to the frontend (separate CRM/admin app vs user-facing app) and finishes the backend split, while preserving identical functionality and UI/UX — this is a pure architectural separation, not a rewrite.

Two decisions were locked in with the user before this plan was written:
- **Backend hosting**: both new backend services follow the exact `signals-service/` pattern — Cloudflare Container + Durable Object wrapper around an unchanged FastAPI app in a Docker container — not native Python Workers (native Workers can't hold a real psycopg2/SQLAlchemy TCP connection, and this pattern isn't proven anywhere in the repo).
- **Repo layout**: a formal pnpm workspace monorepo for the frontend (`apps/`, `packages/`); Python backend services remain plain sibling folders (Python has no pnpm equivalent) alongside the existing `signals-service/`.

Current state, verified by reading the code (not assumed):
- **Frontend** (`frontend/`, Next.js 16 App Router, npm) is *already* route-separated: `app/[locale]/admin/` (25 pages, own RBAC-guarded layout), `app/[locale]/(user)/` (16 pages), `app/[locale]/(auth)/` (user portal's login/register/forgot-password — `admin/login` is its own separate page, not shared with `(auth)`). Everything talks to one backend through one proxy route (`app/api/proxy/[...path]/route.ts` → `helpers/backendUrl.ts`).
- **Backend** (`backend/app/`, FastAPI, SQLAlchemy, no Alembic — ~90 idempotent `ALTER TABLE` statements run inline at import time in `main.py`) has 28 routers. Most are cleanly admin-only or user-only; five are mixed (`mt5_accounts.py`, `referrals.py`, `withdrawal_requests.py`, `forum.py`, `brokers.py`) and need splitting within the file. `require_roles()` is copy-pasted in 19 router files. `auth.py`'s `/login` already has the split's real seam: a `portal` field, verified in code (`backend/app/routers/auth.py:320-352`) — admin-role accounts can only log in on `portal="admin"`, plain users only on `portal="user"`, and `role="client"` accounts can use **either** (this cross-portal case is the one place the split can silently change behavior — flagged for explicit testing below).
- **`signals-service/worker/index.ts`** is cron-only today — its `fetch()` handler is a static placeholder (verified: lines 59-64 return a fixed string). CRM/User backends need real HTTP proxying, which is new code, not a copy of this file.

Goal of this plan: strangler-fig migration — working software at every step, old monolith and new split running in parallel, cutover only after verified parity, and every phase individually revertable until the final decommission.

## Phase 0 — Safety net (no code changes) — ✅ DONE

- Tag `main` as `pre-split-baseline` for rollback. — **done, tag exists.**
- Build a parity fixture: script every one of the ~132 backend endpoints against seeded data and snapshot responses (`backend/tests/` currently only covers 3 files, so this fixture — not the existing suite — is the real regression oracle for the rest of the plan). Store outside any deploy path — **done: `tools/parity/backend_snapshot.py`.**
- Smoke-script the 44 frontend pages (25 admin + 16 user + 3 auth) — one render + one write action each. — **done: `tools/parity/frontend_smoke_checklist.md`** (checklist form, not yet run).

## Phase 1 — Monorepo scaffolding, zero behavior change — ✅ DONE

- `git mv frontend apps/frontend`; add root `pnpm-workspace.yaml` (`packages: ["apps/*", "packages/*"]`) and root `package.json`. — **done, committed in `6359f2a`.**
- Convert `apps/frontend` from npm to pnpm (`package-lock.json` → `pnpm-lock.yaml`); verify `pnpm --filter frontend dev` behaves identically on port 5000. — **done.** (Note: the repo briefly had *no* lockfile at all between the npm removal and this step — closed by running `pnpm install` at the workspace root, which generates a single root-level `pnpm-lock.yaml` for the whole workspace, not a per-package one. Verified `pnpm --filter frontend dev` still serves on port 5000.)
- Create empty skeletons only: `crm-backend/app/`, `user-backend/app/`, `backend-shared/backend_shared/` — not wired into anything yet. — **done** (each is just a directory + empty `__init__.py`, no logic).

## Phase 2 — Extract `backend-shared/`, backend still single-deployed — MECHANICAL MOVE DONE, DB VERIFICATION PENDING
Prove the shared-code boundary by making the *existing* monolith consume it first.
- Move into `backend-shared/backend_shared/`: `database.py`, all 28 model files, all 25 schema files, the 9 service files (`r2_storage.py`, `cloudflare_kv.py`, `stripe_client.py`, `metaapi_*`, `copyfactory_*`, `rebate_calculation.py`, `withdrawal.py`), the 14 util files. — **done** (`git mv`, history preserved).
- New `backend_shared/auth/rbac.py`: single `require_roles()` replacing all 19 copies. — **done.** All 19 were byte-identical (verified by hash before collapsing).
- New `backend_shared/auth/login_service.py`: the portal-aware authenticate logic extracted from `auth.py:320-352` as plain functions (routers stay per-service in Phase 3). — **done.** `auth.py`'s `/login` route now just calls `login_service.authenticate(db, email, password, portal)`.
- New `backend_shared/migrations.py`: the entire inline `ALTER TABLE` block from `main.py`, wrapped in a Postgres advisory lock (`pg_advisory_lock`). This is required, not optional — once both new services call it independently at cold start, concurrent DDL against the same shared Postgres can race. Prove the wrapped version is behaviorally identical here, before two callers exist. — **done**, using `pg_advisory_xact_lock` (transaction-scoped, auto-releases on commit/rollback — simpler than the session-scoped `pg_advisory_lock` the plan named, no manual unlock to leak). All 92 original statements preserved verbatim (diffed to confirm).
- `backend/` imports from `backend_shared` instead of its local copies; `requirements.txt` gets a `-e ../backend-shared` path dependency. — **done.** Added a minimal `backend-shared/pyproject.toml` (setuptools, package auto-discovery) so the editable install resolves.
- **Verify**: rerun the Phase 0 parity fixture — must match byte-for-byte. Run `run_startup_migrations()` twice against a scratch DB to confirm the lock wrap didn't break idempotency. — Initially **deferred by explicit user decision (2026-09-04)**. Later that same day, **actually done for real, against production, by explicit user instruction** ("run the backends"): started `backend`, `crm-backend`, and `user-backend` simultaneously against the real Neon database with real secrets. This is a stronger test than the plan's own "twice against a scratch DB" ask — three near-simultaneous cold-start callers racing for the same advisory lock key, against production, not a scratch copy. All three reached `Application startup complete` cleanly; each service's `/health` returned `{"status":"ok"}`; each new frontend's proxy route reached its own new backend correctly (`crm-frontend → crm-backend`, `user-frontend → user-backend`, both **200** on `/api/proxy/public/homepage`); `apps/frontend → backend` (the original path) still **200** too. Formal byte-for-byte parity-fixture diffing (Phase 0's tool) was still not run — this was a live concurrency + smoke test, not that specific artifact.
- **Real bug found and fixed by this test, invisible to every prior check**: `database.py`'s bare `load_dotenv()` searches upward from *its own file's location* (`backend-shared/backend_shared/`) by default, not from the running service's working directory. Since `backend-shared` is a sibling of `backend/`/`crm-backend/`/`user-backend/` — not their ancestor — the search never reached any of their `.env` files, so all three failed at import time with `DATABASE_URL environment variable is required`. Every earlier verification in this plan set `DATABASE_URL`/`JWT_SECRET` explicitly as env vars for the test itself, so this path was never exercised. Fixed to `load_dotenv(find_dotenv(usecwd=True))` (`usecwd` is a `find_dotenv()` parameter, not `load_dotenv()`'s — first attempt at the fix got that wrong too, caught by the very next run). Single fix point in `backend_shared/database.py` is sufficient for every env var across the whole app, since it's the first module imported everywhere and `load_dotenv()` populates `os.environ` process-wide.
- Separately, first attempt at actually starting the backends also failed on `ModuleNotFoundError: No module named 'backend_shared'` — nobody had ever run a real `pip install -e ./backend-shared` into an actual Python environment; every earlier import check used an ad-hoc `PYTHONPATH` override, not a real install. Fixed by installing it into the same Python 3.11 environment `backend/`'s other pinned dependencies already live in (found by tracing which interpreter the `uvicorn` script actually resolves to, not assumed).
- **Gap found, not in original plan, resolved 2026-09-04**: `backend/docker-compose.yaml`'s commented-out `build:` was correctly identified as unused — confirmed by the user: `backend/` deploys to **Render**, directly from `backend/Dockerfile`, nothing else. That surfaced a real, currently-live-affecting bug: `backend/Dockerfile` still did `COPY requirements.txt .` / `COPY . .` with a build context scoped to `backend/` alone — since Phase 2, `requirements.txt` has `-e ../backend-shared`, which can't resolve inside that context. **This would break the next Render deploy of `backend/` outright** (not a future-migration concern — `backend/` is still the only thing serving live production traffic right now). Fixed with the same pattern already applied to `crm-backend`/`user-backend`: build context = repo root, `COPY backend-shared ./backend-shared` + `COPY backend ./backend` (preserves the original `COPY . .`'s broad copy of everything in `backend/`, not narrowed to just `app/`), `WORKDIR /app/backend` before `pip install`. **Verified by actually building it**: `docker build -f backend/Dockerfile -t backend-test .` from the repo root succeeds, `backend-shared` installs correctly as an editable package. On Render's side, the user needs to set the service's Root Directory to the repo root and Dockerfile Path to `backend/Dockerfile` (exact field names not verified against Render's dashboard — Claude has no access to it).

## Phase 3 — Stand up `crm-backend/` and `user-backend/` (staging DB only, no prod traffic) — CODE DONE, STAGING DEPLOY NOT DONE
- Scaffold both from the `signals-service/` template (`app/`, `worker/`, `wrangler.toml`, `Dockerfile`, `requirements.txt` = backend's deps + `-e ../backend-shared`). — **done.** `worker/index.ts` for both is Container-class boilerplate only (mirrors signals-service) with a `501` placeholder `fetch()` — the real request-forwarding handler is explicitly Phase 4 work, not built here. `Dockerfile` likewise doesn't attempt to solve the `backend-shared` sibling-folder build-context problem (`-e ../backend-shared` won't resolve inside a build context scoped to just this directory) — flagged inline in both Dockerfiles, deferred to Phase 4 exactly as this plan already says to.
- **Clarified 2026-09-04 (explicit user decision, overriding the literal "git mv" wording below): COPY, don't move.** `backend/app/routers/*` stays fully intact and unchanged — the old monolith keeps serving 100% of its routes until Phase 7's actual cutover. A literal `git mv` here would strip these routes out of `backend/` immediately in this branch, which is a production-outage risk if that branch is ever deployed before Phase 7 — contradicts this plan's own "old monolith and new split running in parallel" framing. So everywhere below, "move" means "copy the router file's logic into the new service," not `git mv`. Verified via GitNexus `detect_changes` afterward: zero changes under `backend/app/routers/` from this phase.
- Copy unambiguous routers (source files in `backend/app/routers/` are left untouched): — **done**, all 19 + `public.py`/`geo.py` duplicated into both.
  - **crm-backend**: `clients.py, campaigns.py, articles.py, analysis.py, ad_banners.py, broker_placements.py, seo_meta.py, symbol_categories.py, rebate_payouts.py, media.py, market_prices.py, broker_reports.py, plays.py, copy_traders.py, visits.py, users.py`
  - **user-backend**: `copy_subscriptions.py, notifications.py, billing.py`
  - `public.py`, `geo.py` → duplicated into both (anonymous, consumed by both portals).
  - `internal.py` → `/purge-cache` duplicated into both; `/sync-metaapi`, `/keep-alive-copytrading`, `/sync-subscriptions` → user-backend only (all touch mt5/copyfactory/stripe). — **done** — crm-backend's copy is trimmed to just `_require_sync_auth` + `/purge-cache`; user-backend keeps the full original file (all 4 endpoints).
- Split the 5 mixed files (grep `@router\.` in each first to get authoritative endpoint lists before moving): — **done**, verified against actual role-check code (`require_roles(...)` per endpoint), not endpoint names alone.
  - `mt5_accounts.py`: `/me*` → user-backend; `/admin`, `/active-count` (read-only) → duplicated into crm-backend. — user-backend got the full original file (7 endpoints); crm-backend got a trimmed 2-endpoint duplicate (`/admin`, `/active-count`, both already `require_roles({"super_admin","broker"})`-gated).
  - `referrals.py`: `/me*` → user-backend; `/admin/stats` → crm-backend. — clean disjoint split (no duplication — the plan's own wording omits "duplicated" here, unlike the other four), confirmed by role checks: `/me*` is `require_roles({"client"})`, `/admin/stats` is `require_roles({"super_admin","broker"})`.
  - `withdrawal_requests.py`: `/me` create/list → user-backend; review/approve/reject → crm-backend. — clean disjoint split, same reasoning. crm-backend's half also carries `GET ""` (list-all), bundled with review/approve/reject since it's the admin queue those actions operate on.
  - `forum.py`: full CRUD → user-backend; moderation-only endpoints → duplicated into crm-backend. — user-backend got the full original file (9 endpoints); crm-backend's duplicate started as **`pin_thread` only** (the one endpoint with no self-authorship bypass) but was expanded 2026-09-04 on explicit user request: `update_thread`, `delete_thread`, and `delete_reply` also duplicated in, keeping their original "author or admin" check unchanged (not narrowed to admin-only) — the admin dashboard now has real thread/reply moderation tools, not just pin. Verified: compiles, imports cleanly under the real interpreter, `detect_changes` shows exactly the 3 new functions touched, zero affected processes, risk `low`.
  - `brokers.py`: full CRUD → crm-backend; read-only list/detail + rating submission → duplicated into user-backend. — crm-backend got the full original file (7 endpoints); user-backend's duplicate has 4. **Flagging an oddity found while doing this, not introduced by it**: `list_brokers`/`get_broker` in the duplicate keep their original `require_roles({"super_admin","broker"})` gate — despite the plan calling this pair "read-only list/detail" as if user-facing, a plain `user`/`client` role cannot call them today. Preserved as-is (no behavior change), but worth confirming against actual user-frontend usage before wiring it up in Phase 6.
- `auth.py` becomes two thin routers calling `backend_shared.auth.login_service`: crm-backend gets `/login` (admin), `/forgot-password`, `/reset-password`, `/change-password` — no `/register` (self-registration always creates `role="user"`). user-backend gets the full set including `/register` and OTP. Both must share the same JWT secret so a token minted by either is valid on both (needed for the `role="client"` dual-portal case). — **done**, with two clarifications: (1) `/change-password` isn't a real `auth.py` endpoint — it's `PATCH /users/me` in `users.py` (self-service password change, `current_password`+`new_password`), already present in crm-backend via its full `users.py` copy from the unambiguous-router step above, so nothing extra was needed for it. (2) crm-backend's thin router also includes `/resend-password-reset-otp` (not explicitly named in this plan bullet) as the same reset flow's resend step — an interpretation, flagged rather than assumed silently. `GET /me` (the `/auth/me` variant, distinct from `/users/me`) was deliberately left out of crm-backend's thin router since `/users/me` already covers it.
- Deploy both to a **staging** environment against a staging Postgres — production DB is untouched through this phase. — **NOT done.** Needs Cloudflare account access/credentials Claude doesn't have, and `wrangler deploy` is a real deployment action requiring explicit user execution regardless.
- **Verify**: parity fixture against staging for each service's routes; explicitly test an admin JWT rejected by user-backend's admin-only endpoints (same 403 shape as today), and a `role="client"` JWT working against both services' `/login`; run both services' migration function back-to-back against a fresh staging DB to confirm the advisory lock holds under near-simultaneous cold start. — **NOT done** (depends on the staging deploy above). What *was* verified without a DB or deployment: every new file byte-compiles; imports cleanly under dummy env vars for both services (crm-backend 22/25 routers, user-backend 7/12 — remaining failures are the same pre-existing missing `boto3`/`metaapi_cloud_sdk` packages as Phase 2, unrelated to this work); GitNexus `detect_changes` shows zero affected execution flows, risk `low`, and confirms `backend/` itself has zero changes from this phase.

## Phase 4 — Cloudflare wiring per service — CODE DONE, DEPLOY/SECRETS NOT DONE
- `wrangler.toml` for each, modeled on `signals-service/wrangler.toml` (`[[containers]]`, `[[durable_objects.bindings]]`, `[[migrations]] new_sqlite_classes`) — no `[triggers]` cron block, these are request-driven. — **done** (scaffolded in Phase 3, `image_build_context` added here — see below).
- `worker/index.ts` for each: **new** `fetch()` handler forwarding arbitrary method/path/body into the container via `getContainer(env.X_CONTAINER).fetch(request)` — unlike `signals-service`'s cron-only placeholder. Verify the exact `@cloudflare/containers` forwarding API at implementation time (the signals-service file's own comment flags this SDK as still-evolving). — **done.** Both now just do `return getContainer(env.X_CONTAINER).fetch(request)` — forwards the whole request object (method/path/headers/body) unchanged. The "still-evolving SDK" caveat is carried forward in both files' comments, unresolved — genuinely needs verification at `npm install`/`wrangler deploy` time, which Claude can't do.
- `Dockerfile` for each: copy `signals-service/Dockerfile`'s structure, with the real backend dependency set; resolve the `backend-shared` sibling-folder build-context question once, apply identically to both. — **done, researched not guessed**: `wrangler.toml` now sets `image_build_context = "../"` (repo root) per Cloudflare's documented `[[containers]]` field (defaults to the Dockerfile's own directory). Each Dockerfile now `COPY`s `backend-shared/` alongside its own service directory, preserving the exact same sibling-folder layout the repo has on disk — so `requirements.txt`'s `-e ../backend-shared` line resolves identically in Docker and in local dev, no file needed to change for Docker specifically. Caveat: Cloudflare's docs describe `image_build_context`'s default but don't show a worked example with a parent-directory (`../`) value, so the actual Docker build (context = repo root, same as `image_build_context = "../"` relative to each wrangler.toml) was run locally to confirm it for real — **verified 2026-09-04: both `docker build -f crm-backend/Dockerfile -t crm-backend-test .` and the user-backend equivalent succeed from the repo root**, `backend-shared` installs correctly as an editable package inside the image via the preserved sibling layout. This doesn't prove Cloudflare's own `wrangler deploy` build step behaves identically to a plain local `docker build` — that's still worth a `wrangler deploy --dry-run` before a real deploy — but the Dockerfile/COPY structure itself is now proven, not just theorized.
- Secrets: `DATABASE_URL` (shared Postgres, same as today), JWT secret on both, MetaApi/Stripe sync keys on user-backend only. — **NOT done.** Needs `wrangler secret put` against a real Cloudflare account Claude has no access to, run by the user.
- **Environment designation, decided 2026-09-04**: the `.env` files currently in `crm-backend/` and `user-backend/` (local only, gitignored, not committed) are designated **staging**, per explicit user instruction. Production secrets will be provided separately by the user later — do not assume the current local `.env` values are production-ready, even though most of them (matching `backend/.env`, verified earlier) technically work against the real database.
  - **R2 credentials swapped to a dedicated staging token, 2026-09-04**: `R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY` in both `crm-backend/.env` and `user-backend/.env` were updated to a newly-created R2 API token (Cloudflare dashboard → R2 → Manage API Tokens → Create Account API Token, scoped "read/write/list on all R2 buckets on this account"). Its account hash is `fcd66c6f43690c9559a1ef45c846c77f` — a **different Cloudflare account** than the original `4e9f5ac0b3cff1249cac5ae8fd213b5d`, confirming this is a dedicated staging account, not just a new token on the same account. `R2_BUCKET_NAME` (`tradeverse`) and `R2_PUBLIC_URL` (the old account's `pub-3ce3af24...r2.dev`) were left unchanged since no new values were given — **these almost certainly don't exist under the new account yet** (a bucket named `tradeverse` needs creating there, and its own "Public Development URL" needs enabling to get a real `R2_PUBLIC_URL`). Media uploads (`media.py`, `broker_reports.py`, `forum.py` image posts) will fail against staging until this is resolved.
  - **KV namespace updated, same day**: `CF_KV_NAMESPACE_ID` updated to `dc30d27bb4ed4bc9927ba91083538610` (from a Worker binding config the user pasted — that config style is for native Workers using `env.KV.get()`, which this Python backend can't use; it calls Cloudflare's KV REST API directly instead). `CF_ACCOUNT_ID` updated to `fcd66c6f43690c9559a1ef45c846c77f` — **inferred to match the new R2 account, not explicitly confirmed by the user**, flagging in case that's wrong. `CF_KV_API_TOKEN` is **still the old token, unchanged** — no new one was provided, and the old token is very unlikely to authenticate against the new account's namespace (Cloudflare API tokens are account-scoped). Practical effect until a new token is provided: `utils/cache.py`'s own design means this fails soft, not hard — public-response caching just silently no-ops (every request recomputes) rather than crashing, so staging is still usable, just uncached.
- Still pointed at staging DB.
- **Verify**: `wrangler deploy --dry-run`, then real staging deploy, rerun the parity fixture against the live staging URLs (catches container/DO-specific issues — header stripping, file-upload streaming for `media.py`/R2). — **NOT done**, same reason as Phase 3's verify step: needs Cloudflare credentials and is a real deployment action requiring explicit user execution regardless of access.

## Phase 5 — Extract `packages/frontend-shared`, frontend still single-deployed — DONE, VERIFIED VIA REAL DEV SERVER
Mirror of Phase 2 for the frontend.
- **Component count corrected 2026-09-04**: the "51 live of 59 total" figure above was stale — actual count at execution time was **32 total, 24 live, 8 dead**. The 8 dead ones (`Table, Form, Pagination, StatusLabel, DateFilter, SearchInput, DropownFilter, Icon`) were re-confirmed dead by a fresh grep (0 imports outside themselves) before deleting, per this bullet's own instruction not to trust the earlier scan — that part of the original scan was still accurate, just the total/live counts were off.
- Move the 24 live components, `contexts/`, `helpers/`, `i18n/`, `messages/`, `styles/` into `packages/frontend-shared/src/`. — **done** (`git mv`, history preserved; the 8 dead ones were `git rm`'d, not moved).
- Prefer package export subpaths that match the existing `@/` alias shape so `apps/frontend`'s import statements don't need to change — minimizes the diff on the app still serving 100% of traffic. — **done, but this needed more than the plan's framing implied.** A raw `@/*` alias redirect alone isn't sufficient for a pnpm + Next.js monorepo:
  1. `packages/frontend-shared` needed its own `package.json` (mirroring `apps/frontend`'s dependency list) — pnpm's isolated `node_modules` won't let source files physically outside `apps/frontend/` resolve `react`/`next`/etc. otherwise.
  2. `apps/frontend/package.json` needed `"frontend-shared": "workspace:*"` added as a dependency — required for `transpilePackages` (next) to resolve the package by name.
  3. `next.config.ts` needed `transpilePackages: ["frontend-shared"]` — without it, Next.js treats the moved source as an ordinary external package and skips transpiling its JSX/TSX, breaking the build the moment anything imports it.
  4. `apps/frontend/tsconfig.json` got explicit `@/components/*`, `@/contexts/*`, `@/helpers/*`, `@/i18n/*`, `@/messages/*`, `@/styles/*` path entries pointing into `../../packages/frontend-shared/src/*` (ahead of the generic `@/*` fallback, so they win) — this is the part that actually keeps every existing `@/...` import statement unchanged.
  5. Two non-`@/`-alias references needed manual fixes since they don't go through the alias at all: `next.config.ts`'s `createNextIntlPlugin("./i18n/request.ts")` (a plain string path, updated to the new relative location) and `middleware.ts`'s two `./i18n/...` relative imports (switched to `@/i18n/...`). `i18n/request.ts`'s own `../messages/${locale}.json` dynamic import needed no change — `i18n/` and `messages/` moved together as siblings, so that relative relationship held automatically.
- **Verify**: rerun the Phase 0 frontend smoke script against `apps/frontend`, confirm build output is equivalent. — **Partially done.** Full 44-page smoke script not run (that's Phase 0's manual/scriptable checklist, not something run here). What *was* verified: ran `pnpm install` to link the new workspace package (confirmed via the `node_modules/frontend-shared` symlink), then actually started `pnpm --filter frontend dev` and requested `/en` for real. First attempt failed — `Module not found: Can't resolve '../../styles/reset.scss'` from `app/[locale]/layout.tsx`, a bare side-effect `import "../../styles/reset.scss"` with no `from` keyword that an initial grep (which required `from`) missed entirely. Fixed to `import "@/styles/reset.scss"`; broadened the grep (dropped the `from` requirement) and found no other instances in either direction (app/ referencing moved dirs, or moved dirs referencing app/assets/public/data). Re-requested `/en` after the fix: **200 OK**. GitNexus `detect_changes` afterward: 3 real changed symbols (all expected), zero affected processes, risk `low`. **One more gap found after that**: `global.d.ts` had `import type messages from "./messages/en.json"` — a same-directory (`./`, not `../`) reference the broadened grep still didn't catch since it was only searching `\.\./` patterns. `next dev`'s runtime bundling doesn't type-check `.d.ts` files, so the 200 OK above didn't surface this — only running an actual `tsc --noEmit` did. Fixed to `@/messages/en.json`; a final grep covering both `./` and `../` in one pattern found nothing else; `npx tsc --noEmit -p tsconfig.json` now exits 0 clean. Lesson carried into Phase 6: a passing dev-server request proves runtime resolution, not type-checking — both are needed.

## Phase 6 — Stand up `apps/crm-frontend` and `apps/user-frontend` (staging backends, not yet prod) — DONE, VERIFIED VIA REAL DEV SERVERS
- **COPY, not `git mv`, same reasoning as Phase 3**: `apps/frontend` is still the only thing actually deployed anywhere; a literal `git mv` of `admin/`/`(user)/`/`(auth)/` out of it would break production the moment this branch deploys, before Phase 7's real cutover. Applied without re-asking since this exact question was already settled for the backend split.
- `apps/crm-frontend`: **copied** `app/[locale]/admin/` (23 items: 21 route dirs + its own `layout.tsx` + `page.tsx`) + its own `app/api/proxy/[...path]/route.ts` pointed at crm-backend (via `BACKEND_URL`, no code change needed).
- `apps/user-frontend`: **copied** `app/[locale]/(user)/` + `app/[locale]/(auth)/` (login/register/forgot-password) + its own proxy route pointed at user-backend.
- **Gap found, not in the original plan**: `app/[locale]/page.tsx` + `HomeClient.tsx` + `page.module.scss` — the public homepage — sits at the `[locale]` root, outside all three named route groups, and neither this phase nor any other mentions it. Placed with `apps/user-frontend` (public/anonymous page, no admin relevance) — flagging the call rather than assuming it's uncontroversial.
- Auth routing needs no new abstraction: `admin/login` is already its own self-contained page (doesn't reuse `(auth)/login`), so this is a clean move, not a shared-wrapper redesign. — confirmed `LoginModal` (in frontend-shared) is never imported from any admin page (fresh grep, zero hits) — genuinely user-only as assumed.
- `backendUrl.ts` already reads `BACKEND_URL` from env with no hardcoding — per-app env var is enough, no code change needed there. — confirmed, untouched.
- Read `apps/frontend/app/[locale]/layout.tsx` (the true root layout, above both `admin/` and `(user)/`) before duplicating it, to confirm which chrome is shared vs sub-layout-owned. — read and copied as-is into both new apps (html/body shell, theme-init script, `NextIntlClientProvider`/`ThemeProvider`/`AuthProvider`) — `admin/layout.tsx` and `(user)/layout.tsx` are each app's own already-existing sub-layout, copied along with their route group, untouched.
- Full scaffold needed beyond just the pages (none of this was itemized in the plan, same category of gap as Phase 5's monorepo plumbing): `package.json` (mirrors `apps/frontend`'s deps + `frontend-shared: workspace:*`, dev ports 5001/5002 to avoid colliding with `apps/frontend`'s 5000), `tsconfig.json`/`next.config.ts`/`middleware.ts` (identical structure, same relative depth to `packages/frontend-shared`), `.gitignore` (copied from `apps/frontend` — needed so `next-env.d.ts`/`*.tsbuildinfo`/`.next/` don't get committed; missing this was caught while reviewing `git status` before this section was written), `postcss.config.mjs`, `eslint.config.mjs`, `global.d.ts`, `Dockerfile`, `public/` (9KB, copied whole into both). `assets/` copied into `crm-frontend` only (only 2 admin pages reference it via `@/assets/...`, confirmed by grep; `user-frontend` needs none).
- **Real bug found and fixed, not anticipated by the plan**: `CampaignCard` (moved to `packages/frontend-shared` in Phase 5) itself imports two icons via `@/assets/icons/...`. This was invisible in Phase 5's verification because `apps/frontend` still has its own local `assets/` folder masking the problem. First attempted fix (adding `@/assets/*` → `frontend-shared/src/assets/*` to `tsconfig.json`, mirroring the components/contexts/etc. pattern) type-checked clean (`tsc` exit 0) but **broke at the webpack/runtime level** — TypeScript's path resolution falls through to the next matching pattern when a file doesn't exist, webpack's tsconfig-paths resolution does not, so the admin-only icons (which only exist locally, not in frontend-shared) stopped resolving the moment that override was added. Reverted the tsconfig change; fixed `CampaignCard` itself to use a plain relative import to its sibling `assets/icons/` inside `packages/frontend-shared` instead — works identically regardless of which app imports it, no alias trickery required. Only caught because routes were tested against the real dev server, not just type-checked — logged here as a concrete instance of the Phase 5 lesson ("passing dev-server request proves runtime resolution, not type-checking — both are needed") holding in reverse too: **passing type-check doesn't prove runtime resolution either.**
- **Verify**: split smoke script (25 admin pages against crm-frontend, 16 user + 3 auth pages against user-frontend), both against staging backends. Explicitly test the `role="client"` cross-portal case end-to-end, and flag to the user: physically separating the apps may turn what was previously an in-app view into a cross-origin navigation for that one user type, depending on final domain strategy — this is the one place "identical UI/UX" needs a conscious decision rather than an assumption. — **Partially done**, same caveat as Phase 5: full smoke script and the staging-backend / cross-portal JWT test need real backend deploys (Phase 3/4's blocker). What *was* verified: `tsc --noEmit` clean (exit 0) for `apps/frontend`, `crm-frontend`, and `user-frontend` after the `CampaignCard` fix; then real dev servers started for both new apps and a broad sample of real routes hit — crm-frontend: `/admin/login`, `/admin/account`, `/admin/contact-us` (the two pages needing the fixed icons), `/admin/overview`, `/admin/users`, `/admin/clients`, `/admin/articles`, `/admin/brokers`, `/admin/withdrawal-requests` — all 200. user-frontend: homepage, all 3 auth pages, and 9 of 11 `(user)/` routes — all 200; `/brokers` (bare) 404s, confirmed to match `apps/frontend`'s original structure exactly (no `page.tsx` there either — not a bug). One transient "Jest worker" crash hit while firing 3 routes at once against a freshly-started dev server — reproduced cleanly with the same routes tested individually afterward, confirming it was resource contention from this session's cumulative build/compile load, not a real code issue. GitNexus `detect_changes`: zero affected processes, risk `low`.
- The `role="client"` cross-portal case (a user with `role="client"` needing to authenticate against *both* crm-backend and user-backend `/login` with the same JWT) and the cross-origin-navigation UX question are both still open — genuinely need a staging deploy to test end-to-end, not something verifiable from source alone.
- **Gap found and closed 2026-09-04, not itemized anywhere in the plan**: Phase 6 scaffolded the Next.js apps themselves but never wired up *how* they actually deploy to Cloudflare — no `wrangler.toml`, no adapter. Researched fresh rather than assumed: Cloudflare's current docs recommend `vinext` (a beta Vite-based reimplementation of Next.js) as the new default, with `@opennextjs/cloudflare` (GA since Feb 2026) as the fallback for apps that can't migrate yet. **User chose `@opennextjs/cloudflare`** for maturity/production-readiness over the beta tool.
  - Installing it surfaced a real peer-dependency conflict: `@opennextjs/cloudflare@1.20.6` requires `next@">=15.5.24 <16 || >=16.3.3"` — this project was pinned to `next@16.0.10`, squarely inside the unsupported gap. **User chose to upgrade Next.js to `16.3.4`** (latest stable satisfying the constraint) across all three frontend apps (`apps/frontend`, `crm-frontend`, `user-frontend`) rather than risk building on an explicitly-unsupported combination.
  - Upgrade verified properly: `tsc --noEmit` failed at first with `PrefetchForTypeCheckInternal` errors in all three — traced to stale `.next/dev/types/` caches generated by the old Next.js version, not a real code break. Cleared all three `.next/` directories, reran — clean (exit 0) on all three. Then real dev servers started for all three and hit for real (`/en`, `/en/admin/login`, `/en`) — all **200**, no errors in any log.
  - `@opennextjs/cloudflare` install itself had a separate glitch: it physically installed into `apps/user-frontend/node_modules/@opennextjs/cloudflare` but never wrote the `dependencies` entry into `package.json` (likely a side effect of the peer-conflict rejection at the time) — added by hand afterward to match what was actually on disk.
  - **`user-frontend`'s OpenNext setup completed and verified working, 2026-09-04**: `wrangler.jsonc`, `open-next.config.ts`, `next.config.ts`'s `initOpenNextCloudflareForDev()` call, `cf:preview`/`cf:deploy`/`cf:upload`/`cf:typegen` package.json scripts, `.dev.vars`, `public/_headers`, and `.gitignore` entries (`.open-next/`, `.wrangler/`, `.dev.vars`, `cloudflare-env.d.ts`) all added.
  - **Real build failure hit and root-caused, not anticipated**: `npx opennextjs-cloudflare build` failed three times in a row with the identical esbuild error — `No loader is configured for ".node" files` trying to bundle `sharp`'s compiled native binary. `next/image` isn't used anywhere in this app (confirmed by grep across `user-frontend` and `frontend-shared`), so this was Next's own optional image-optimizer pulling `sharp` in unused, and Cloudflare Workers can't run native binaries regardless of whether it's actually invoked. Two plausible-looking fixes were tried and **both failed** because they act at the wrong layer: `images: { unoptimized: true }` (doesn't stop the module from being statically bundled) and `serverExternalPackages: ["sharp"]` (only affects Next's own build, not OpenNext's separate esbuild pass over the already-built output). This is a confirmed, currently-unresolved upstream limitation — esbuild statically resolves `require()` to native `.node` files even inside try/catch, and OpenNext's maintainers have not yet exposed a supported config surface for it ([opennextjs-cloudflare#1226](https://github.com/opennextjs/opennextjs-cloudflare/issues/1226), same class of bug with a different package). **Actual fix**: root `package.json`'s `pnpm.overrides` — `"sharp": "npm:empty-npm-package@1.0.0"`, replacing the real `sharp` with a real, published no-op stub across the whole workspace (safe repo-wide since no app anywhere uses `next/image`, re-confirmed by grep before applying). `images.unoptimized` and `serverExternalPackages` were left in place too, since they're correct/harmless on their own even though neither alone was sufficient.
  - **Verified for real, not just "build succeeded"**: `npx opennextjs-cloudflare build` completed cleanly (`Worker saved in .open-next/worker.js`), then `npx wrangler deploy --dry-run` from `user-frontend/` also succeeded — `Total Upload: 7719.52 KiB / gzip: 1517.58 KiB` (well under Cloudflare's bundle limits), bindings (`WORKER_SELF_REFERENCE`, `ASSETS`) correctly recognized. No actual deploy was performed — dry-run only.
  - **Still remaining**: `crm-frontend` and `apps/frontend` need the identical OpenNext setup (not yet done — only `user-frontend` has it) now that the sharp workaround is known and proven. R2-backed incremental cache (`open-next.config.ts`'s `incrementalCache` option) was deliberately left at OpenNext's default rather than wired to R2 — needs its own bucket/binding decision, not just left as a TODO forever, since a few pages use `{ next: { revalidate } }`.
- **`user-frontend` actually deployed live to Cloudflare, 2026-09-04, by explicit user instruction ("do it")**: `https://user-frontend.tradeversesocial.workers.dev`. Real deploy, not a dry-run.
  - **Hit and fixed a second real runtime bug the build/dry-run didn't catch**: every route 500'd in production with `Error: Dynamic require of "/.next/server/middleware-manifest.json" is not supported` — Next's own `getMiddlewareManifest()` does a dynamic `require()` at request time that Cloudflare's Workers runtime can't execute, and this app's real `middleware.ts` (locale detection/routing) genuinely exercises that path. Confirmed via `wrangler tail` against the live Worker, not guessed — took three attempts to even get `wrangler tail` to actually capture a log line (first two connected but returned nothing, seemingly reusing a stale session — same tail session expiry timestamp shown across attempts). This is a known open upstream bug ([opennextjs-cloudflare#922](https://github.com/opennextjs/opennextjs-cloudflare/issues/922), [#1232](https://github.com/opennextjs/opennextjs-cloudflare/issues/1232)), not fixable in application code. Fix: `"vars": { "NEXT_PRIVATE_MINIMAL_MODE": "1" }` in `wrangler.jsonc` — forces Next.js to skip that code path entirely. The alternative fix some report (`shamefully-hoist=true` in `.npmrc`) was passed over since it changes pnpm's `node_modules` layout for the *entire* monorepo, a much bigger blast radius than a single Worker's env var.
  - **Redeployed and verified for real after the fix**: homepage `/en` — 200, real HTML. Bare `/` — 307 redirect to `/en/` (confirms `middleware.ts`'s locale-detection logic itself still works correctly under minimal mode, not just that it stopped crashing). `/en/login`, `/en/forum` — 200.
  - **1003 issue resolved by deploying the real backend** (see below) rather than chasing the Render-edge rejection further — once `BACKEND_URL` pointed at the real deployed `user-backend`, it went away entirely (was specific to reaching Render from a Worker, not a lasting problem).
- **`user-backend` deployed live to Cloudflare Containers, 2026-09-04, by explicit user instruction ("deploy now the user backend")**: `https://cbfx-user-backend.tradeversesocial.workers.dev`.
  - **Gap found and fixed before first deploy attempt**: `user-backend` had no `package.json` at all — `worker/index.ts` imports `@cloudflare/containers`, which was never actually installed anywhere (Phase 3/4 wrote the worker code but never set up its own npm project, unlike `signals-service`, the template it was modeled on). Added `package.json` (matching `signals-service`'s shape, `@cloudflare/containers@^0.3.7` — latest, not `signals-service`'s pinned `^0.0.15` — plus `wrangler@^4.129.0`), `npm install`'d standalone (not part of the pnpm workspace — `user-backend/` isn't under `apps/*`/`packages/*`).
  - **`wrangler deploy --dry-run` succeeded**: real Docker container build via wrangler itself (not just a manual local `docker build`), `env.USER_BACKEND_CONTAINER` Durable Object binding correctly recognized.
  - **`wrangler secret bulk .env`**: all 24 secrets pushed in one command, auto-created the Worker (`cbfx-user-backend`, matching `wrangler.toml`'s `name`) since it didn't exist yet.
  - **First real deploy attempt failed with a generic `fetch failed`** right after the Docker image successfully built and pushed to Cloudflare's registry — read as a transient network blip given the image was already fully uploaded, not a config problem. A plain retry succeeded immediately (image reused from the registry, no rebuild needed) — deployed to `https://cbfx-user-backend.tradeversesocial.workers.dev`.
  - **`/health` initially returned 500**: `Failed to start container: The container is not running, consider calling start()`. Root-caused, not guessed: `wrangler secret put`/`secret bulk` sets secrets on the *Worker's* own env — that does **not** automatically become the *container's* process environment (a separate Docker sandbox). The FastAPI app inside was crashing immediately on startup (`DATABASE_URL environment variable is required`, same guard `backend_shared/database.py` enforces everywhere else), which surfaced as an opaque container-start failure with zero indication env vars were the cause. Checked the current `@cloudflare/containers` README directly (confirmed the `getContainer(...).fetch(request)` pattern in `worker/index.ts` was already correct/current — the bug wasn't an API mismatch) and found the actual mechanism: a `Container` subclass's `envVars` property, set from the constructor's `env` argument. Rewrote `user-backend/worker/index.ts`'s `UserBackendContainer` with an explicit constructor wiring all 24 secrets from `env` into `this.envVars`. Redeployed (container image reused, unchanged — only the Worker script changed) — `/health` now returns `{"status":"ok"}` for real, confirmed by direct HTTP request, and `/public/homepage` returns 200 with genuine data queried from the real production database.
  - **Same `envVars` fix will be needed for `crm-backend`'s `worker/index.ts`** when that gets deployed — not yet done.
- **`user-frontend`'s `BACKEND_URL` updated to point at the real deployed `user-backend`** (`https://cbfx-user-backend.tradeversesocial.workers.dev`, Worker-to-Worker, no longer falling back to Render) and redeployed. **Verified fully end-to-end for real**: `/api/proxy/public/homepage` returns genuine production data (real market prices, forum threads, broker listings — not a stub) consistently across repeated requests (one single 403 immediately after the first deploy read as a transient cold-start blip, not reproduced on retry); `/en`, `/en/login`, `/en/forum`, `/en/cashback` all 200. **This is the first fully working end-to-end slice of the entire migration** — real browser-facing frontend, on Cloudflare, talking to a real backend, on Cloudflare, talking to the real production database, with zero involvement from `apps/frontend`/`backend/`/Render.
- **`crm-backend` deployed live to Cloudflare Containers, 2026-09-05, by explicit user instruction ("let's do the same for th crm (admin) be and fe")**: `https://cbfx-crm-backend.tradeversesocial.workers.dev`. Replicated the exact `user-backend` sequence with every gotcha applied proactively instead of rediscovered:
  - `package.json` created up front (same shape as `user-backend`'s: `@cloudflare/containers@^0.3.7`, `wrangler@^4.129.0`), `npm install`'d standalone.
  - `worker/index.ts` written with the `envVars` constructor fix already in place (21 secrets wired from `env` into `this.envVars` on `CrmBackendContainer` — no Stripe vars, since `crm-backend` has no billing.py) — the fix that took a full debugging cycle to find for `user-backend` needed zero rediscovery here.
  - `wrangler deploy --dry-run` succeeded first try (bindings recognized, container built). `wrangler secret bulk .env` pushed all 21 secrets, auto-created the Worker. Real `wrangler deploy` succeeded on the first attempt (no transient `fetch failed` retry needed this time).
  - **Verified working on the first deploy** (unlike `user-backend`, which needed a second deploy after finding the `envVars` bug): `/health` → `{"status":"ok"}`, `/docs` → real Swagger UI titled "CBFX CRM API" confirming the correct app is running.
- **`crm-frontend`'s OpenNext/Cloudflare setup completed and deployed live, 2026-09-05**: `https://crm-frontend.tradeversesocial.workers.dev`. `next` was already at `16.3.4` in this app (upgraded alongside the other two apps back in Phase 6), so no version work was needed here. Added: `@opennextjs/cloudflare` dependency + `cf:preview`/`cf:deploy`/`cf:upload`/`cf:typegen` scripts + `wrangler` devDependency to `package.json`; `wrangler.jsonc` (mirroring `user-frontend`'s — `NEXT_PRIVATE_MINIMAL_MODE: "1"` included proactively since this app also has a real `middleware.ts`); `open-next.config.ts` (`buildCommand: "next build --webpack"`, same Turbopack-avoidance reason as `user-frontend`); `initOpenNextCloudflareForDev()` + the `images.unoptimized`/`serverExternalPackages: ["sharp"]` pair added to `next.config.ts`; `.dev.vars`, `public/_headers`, and the `.gitignore` cloudflare/opennextjs block. The `sharp` fix itself needed zero rework — root `package.json`'s `pnpm.overrides` entry is workspace-wide and covered this app automatically.
  - `pnpm install --filter crm-frontend` picked up the new dependency cleanly. `opennextjs-cloudflare build` succeeded (`Worker saved in .open-next/worker.js`) — build-time fetch retries logged against `BACKEND_URL=http://localhost:8001` (from `.env.local`, no local backend running at build time) were benign; Next.js's own fetch-retry logging during SSG for pages using build-time data fetching, not a build failure. `wrangler deploy` uploaded 72 assets and deployed the Worker on the first attempt.
  - **Verified fully end-to-end for real, first try — no repeat of `user-frontend`'s two runtime bugs**: `/en/admin/login` → 200, real HTML (`NEXT_PRIVATE_MINIMAL_MODE` avoided the middleware-manifest crash from the start). `/api/proxy/health` → `{"status":"ok"}`, confirming the proxy route correctly reaches the freshly-deployed `crm-backend` (`BACKEND_URL` was set to `https://cbfx-crm-backend.tradeversesocial.workers.dev` from the very first deploy, so the Render-fallback/error-1003 issue user-frontend hit never occurred here either).
- **Both crm-backend/crm-frontend and user-backend/user-frontend pairs are now fully live on Cloudflare, each talking to the real production database, with zero involvement from `apps/frontend`/`backend/`/Render.** `apps/frontend` and `backend/` remain untouched and fully live in parallel (strangler-fig pattern intact) — no traffic has been cut over yet; that's Phase 7, which needs the user's own infrastructure access.
- **Four verification gaps closed for real, 2026-09-05, against the live deployments** (the pieces of Phases 0/3/4/6's "verify" steps that were previously marked not-done or partial):
  1. **Formal parity fixture actually run**, not just spot-checked: `tools/parity/backend_snapshot.py` executed against all three backends — the old monolith (`https://cbfx.onrender.com`, cold-started from Render's free-tier sleep first), `crm-backend`, and `user-backend` — using real JWTs minted via `login_service.authenticate()` called directly in-process (same code `/auth/login` calls, just bypassing the captcha gate, which is a bot-defense check orthogonal to the response-shape parity being tested). Diffed programmatically: **34 routes on crm-backend and 20 on user-backend, 0 real mismatches** (status code + response-shape identical to the old monolith for every route; only actual data values differ where expected, e.g. timestamps).
  2. **Full 44-page frontend smoke script actually run**, not a manual sample: scripted every admin (25), user (16), and auth (3) page against the live `crm-frontend`/`user-frontend`, plus the homepage — **all 200**, including all 6 dynamic-segment pages (`articles/[id]`, `analysis/[id]`, `forum/[id]`, `news/[id]`, `markets/[symbol]`, `brokers/[id]`) tested with real seeded IDs/symbols pulled live from the backends' own public endpoints. The one known 404 (`/brokers` bare) reconfirmed as matching the original monolith's structure, not a bug.
  3. **`role="client"` dual-portal case tested end-to-end for real**, closing the one behavior-change risk this plan flagged repeatedly since Phase 0. No usable test password existed for the 3 pre-existing client-role accounts, so — **by explicit user instruction ("just create one client role"), after confirming the exact write first** — created one new test account (`test-client@cbfx.com` / `password123`, `role="client"`) via a single direct DB insert (the only DB write performed this session outside the earlier advisory-lock migration test). Immediately verified: `authenticate(db, email, password, portal)` succeeds for **both** `portal="admin"` and `portal="user"` against the real production DB — confirms `login_service.py`'s dual-portal logic (identical code in both crm-backend and user-backend) behaves as documented. Full HTTP-level proof through the actual `/auth/login` endpoint on both deployed Workers would additionally require solving each portal's live reCAPTCHA widget in a real browser, which Claude does not do (prohibited bot-detection bypass) — the in-process call exercises the exact same authorization code path everything else in this verification pass already relied on.
  4. **R2/KV staging gap from Phase 4, investigated and partially closed**: the `tradeverse` R2 bucket on the new staging account turned out to **already exist** (created 2026-09-04, contradicting this plan's earlier "almost certainly doesn't exist yet" guess) — checked via `wrangler r2 bucket list`/`info`, not assumed. Its real public dev URL (`wrangler r2 bucket dev-url get tradeverse`) is `https://pub-17a528d832384afe945788adccf2a7dc.r2.dev` — **a real, previously-undetected bug**: both `.env` files still had the *old* Cloudflare account's dev URL (`pub-3ce3af24...`), which would have produced broken links for every future media upload (forum images, broker reports, media library) despite uploads themselves succeeding. Fixed in both `.env` files, pushed as an updated `R2_PUBLIC_URL` secret to both live Workers via `wrangler secret put`, and both redeployed to force the running containers to pick it up (containers read `envVars` once at startup, not on every request) — `/health` on both confirmed `{"status":"ok"}` post-redeploy. **`CF_KV_API_TOKEN` is still broken** — directly tested against the KV REST API (`GET .../storage/kv/namespaces/{id}/keys`), returns 401. Wrangler's own OAuth session token was tried as a substitute and also 401s (OAuth session tokens aren't valid as generic API bearer tokens for this REST surface). **Genuinely needs the user**: create a new Cloudflare API token (dashboard → My Profile → API Tokens → Create Token, scoped `Workers KV Storage: Edit` on the `fcd66c6f43690c9559a1ef45c846c77f` account) — not something achievable through `wrangler` or any credential already available. Until then, `utils/cache.py`'s fail-soft design means this stays a silent no-op (no caching, no crash), not a functional break.
  - **Resolved same day**: user created the token via the "Edit Cloudflare Workers" template (bundles `Workers KV Storage: Edit` plus Workers Scripts/Routes/Tail — a purpose-built custom token wasn't necessary), scoped to the `fcd66c6f43690c9559a1ef45c846c77f` account only. Verified directly against the KV REST API before pushing anywhere (200, not 401), then updated both `.env` files, pushed as `CF_KV_API_TOKEN` via `wrangler secret put` to both Workers, and redeployed both. **Verified caching actually works end-to-end, not just that the token authenticates**: hit `/public/homepage` on the live `user-backend` twice — first request 3.79s (cache miss, computed fresh), second 1.30s (cache hit); confirmed via the KV REST API directly that a real key (`public:homepage:JO:middle_east`) now exists in the namespace with a live TTL. All four Phase 4 verification gaps (parity fixture, frontend smoke test, dual-portal login, R2/KV) are now fully closed.

## Phase 7 — Cutover (staging → production, per-service, independently reversible) — CANNOT BE EXECUTED BY CLAUDE
This phase is real production traffic routing against real infrastructure Claude has no access to (Cloudflare account, DNS, production secrets) — and even with access, each step below is gated on soak-monitoring and human judgment calls the plan itself requires, not something to automate through. Everything through Phase 6 is code-ready for this; the steps below are a runbook for the user to execute, not a checklist Claude will check off.
1. **Backend first**: point crm-backend/user-backend wrangler configs at the *production* Postgres (safe — same schema/data, just two more clients), deploy to production Cloudflare, soak-monitor before proceeding. Prefer letting the new frontends (Phase 6, already single-backend by design) do the actual traffic cutover rather than bridging the old monolith's proxy route to dual-route by path prefix — avoids writing throwaway bridge code.
2. **Frontend, per portal, independently**: cut the admin route/subdomain to `apps/crm-frontend` first (lower volume, easier rollback), soak, then cut the user portal to `apps/user-frontend`.
3. Leave `apps/frontend` and `backend/` deployed but receiving zero traffic for a bake period — rollback at this stage is a routing revert, not a code revert, and there's zero data-loss risk since both old and new point at the same DB throughout.
- **Prerequisite checklist before starting** (from what this migration built): staging deploy + DB verification from Phases 2-4 actually run and passing (never done this session — see those phases' status); `wrangler secret put` run for `DATABASE_URL`/JWT secret on both new backends (staging first, then production) plus MetaApi/Stripe sync keys on user-backend only; the `role="client"` dual-portal JWT case and the cross-origin-navigation UX question (Phase 6) tested end-to-end against real staging URLs, not just reasoned about from source.

## Phase 8 — Decommission — CANNOT BE EXECUTED BY CLAUDE
Explicitly flagged in this plan as effectively irreversible, gated on an explicit go-ahead after a real bake period — that go-ahead can only come from the user observing actual production behavior post-cutover, not from anything verifiable in this repo. Not attempted.
- Remove old deploy pipelines first (infra-only), confirm clean, *then* in a separate change `git rm -r apps/frontend backend/` (history is preserved either way).
- Update `README.md`/`AGENTS.md`/`CLAUDE.md`/`docs/` references.
- Treat this phase as effectively irreversible — gate it on an explicit go-ahead after the bake period, not just "no errors yet."

## Key non-obvious calls this plan makes explicit
- Migration-block race risk solved with a Postgres advisory lock, not a "one service owns migrations" convention (avoids a hidden deploy-order dependency).
- `require_roles()`: 19 copies → 1 shared dependency.
- `auth.py` split: shared functions, duplicated thin routers; crm-backend drops `/register` entirely.
- Mixed-file splits move the user-owned half, duplicate the admin-owned read/moderation half — same DB, same models, no behavior change, only which process serves it.
- `worker/index.ts`'s `fetch()` handler is new code for both services — the one place this plan isn't a lift-and-shift from `signals-service`.
- The `role="client"` dual-portal case and the frontend's cross-origin-navigation seam are called out explicitly rather than silently assumed away.

## Verification approach throughout
Given `backend/tests/` only covers 3 of ~132 endpoints, the Phase 0 parity fixture (response-shape snapshots against seeded data) is the real regression oracle for every phase — not the existing test suite. Frontend parity uses a scripted smoke pass over all 44 pages. Every phase has a stated rollback point; nothing is one-way until Phase 8.

### Critical files referenced
- `backend/app/main.py` — inline migration block to extract
- `backend/app/routers/auth.py` — portal-aware login logic (lines ~320-352)
- `signals-service/worker/index.ts` — hosting precedent (cron-only today, needs a new fetch handler for the two new services)
- `signals-service/wrangler.toml` — Cloudflare Container/DO config precedent
- `frontend/app/api/proxy/[...path]/route.ts`, `frontend/helpers/backendUrl.ts` — API routing, already split-ready
- `frontend/helpers/roles.ts` — RBAC source of truth, must stay in sync with backend

## Progress log

- 2026-09-03: Discovered this plan had drifted out of sync with a separate,
  less-detailed plan drafted earlier the same session before this file was
  found. Resumed from here per user's explicit choice. Checked this copy into
  the repo (this file) so it stops living only in `~/.claude/plans/`.
